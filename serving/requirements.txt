# UTMOSv2 serving stack. Python 3.10+, 1x NVIDIA GPU recommended (CPU works, slow).
# Install the UTMOSv2 package itself from the repo root:  pip install -e ..
# (that pulls torch / torchaudio / librosa / timm etc. from the project's pyproject).
#
# These are the extra deps the server/benchmark need on top of the package:

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9      # multipart file upload for /predict
httpx>=0.27.0                # benchmark.py client
soundfile>=0.12.1            # libsndfile decode (GIL-releasing)
soxr>=0.5.0                  # fast HQ resample (GIL-releasing); librosa is the fallback
